<!-- Owner: workflow-environment-detection. Only that skill writes this file;
     all other skills read it. Rewritten only if the environment changes. -->

# Environment report

## Capabilities
<!-- git + remote, CI, display/GPU, connected devices/simulators,
     engine + version, engine Editor MCP, required runtimes — present/absent + detail -->

## Verification tier
<!-- the highest tier this environment supports (the ceiling), and what is NOT reachable;
     whether an Editor MCP is in the loop for interactive checks -->

## Provisioning context
<!-- tool_provisioning level; ephemeral/CI/container vs. personal machine -->
