# Integrations

MCP / tracking adapters behind one Producer interface. Local `git` + a committed
`studio/backlog.md` is the zero-config default. GitHub first; Jira, GitLab,
Trello as opt-in adapters. (Phase 3.)
