# Evals

Tiny, fully-specified reference games the framework must take from spec to a
playable Unity build. These are the regression tests for VGDD itself: a change
is accepted only if the suite still builds and passes its verification tier.

`reference-games/` holds one folder per brief (e.g. `snake/`, `match3-slice/`).
(Phase 2.)
